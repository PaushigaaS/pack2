# `dimulpy`

The `dimulpy` is a simple testing example to understand the basics of developing your first Python package.

Demo:

```
from multiply.multiply_by_three import multiply_by_three
from divide.divide_by_three import divide_by_three

multiply_by_three(9)
divide_by_three(21)
```